class Solution:
    def uniformArray(self, nums1: list[int]) -> bool:
        # check if nums1 is not empty
        if len(nums1) < 1:
            return False

        # return true, since all possible list value combinations
        # are guaranteed to return true anyway
        return True

